# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# graphify (a uv tool, not a project dep) — 1.7MB of regenerable output
graphify-out

# Rust build output. CARGO_TARGET_DIR moves this to ~/.cargo-target on this
# machine, but keep the entry so a machine without that env var is still covered.
src-tauri/target

# macOS bundle output
*.app

# Claude Code machine-local state
.claude/settings.local.json
.claude/scheduled_tasks.lock
